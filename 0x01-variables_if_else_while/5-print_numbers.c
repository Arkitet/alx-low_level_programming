#include <stdio.h>

/**
 * main- entry point indicates start of runtime
 *
 * Return: 0 when successful
 */

int main(void)
{
	int i;

	for (i = 0; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
